azure_rm_virtualmachine:
  resource_group: titanstestvm
  name: titantestvm10
  vm_size: Standard_B1
  admin_username: ansibletest
  ssh_password: false
  ssh_public_keys: "{{ ansibletestaz.pem }}"
  image:
    offer: CentOS
    publisher: OpenLogic
    sku: '7.1'
    version: latest
